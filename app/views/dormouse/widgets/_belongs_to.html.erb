<%= div_for(object, property.names.param_id, :class => property.type) do %>
  <%= form.label(property.names.param_id, property.label) if property.label %>
  <%= form.collection_select(property.names.param_id,
        target.resource.all,
        :id, target.primary_name_column, property.options) %>
<% end %>